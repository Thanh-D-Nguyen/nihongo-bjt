# SQL functions (optional)

Reserved for PostgreSQL helper functions (e.g. safe refresh wrappers, partition maintenance).

Reporting views and materialized views in this repo do not require custom functions.
