name: nihongo_bjt
description: "NihonGo BJT — Japanese (BJT) learning app for Vietnamese learners."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  # Type-safe reactive SQLite ORM; backs the offline flashcard cache
  # (core/database + features/flashcards/data/local). Generated code committed.
  drift: ^2.20.0
  flutter:
    sdk: flutter
  # OIDC Authorization Code + PKCE via the system browser (Custom Tabs /
  # ASWebAuthenticationSession). Powers core/auth + Keycloak sign-in.
  flutter_appauth: ^12.0.1
  # Official Flutter localization delegates (Material/Cupertino/Widgets) used by
  # the gen-l10n AppLocalizations. SDK package, no external dependency.
  flutter_localizations:
    sdk: flutter
  # State management + dependency injection.
  flutter_riverpod: ^3.3.1
  # Encrypted token storage (Keychain / EncryptedSharedPreferences) — never
  # plain SharedPreferences. Backs core/auth/secure_auth_token_store.dart.
  flutter_secure_storage: ^9.2.4
  # Declarative routing.
  go_router: ^17.2.3
  # HTTP transport for the core API client (core/api/api_client.dart).
  http: ^1.2.0
  # Message formatting / placeholders for the generated gen-l10n localizations
  # (lib/l10n). Required by flutter_localizations gen-l10n output.
  intl: any
  # JSON serialization annotations for the generated flashcard API DTOs
  # (features/flashcards/data/dto). Codegen via json_serializable/build_runner.
  json_annotation: ^4.12.0
  # `@immutable` annotation for value types (auth domain).
  meta: ^1.18.0
  # Cross-platform path joining for the local Drift database file location.
  path: ^1.9.0
  # Resolves the platform app-documents directory where the Drift SQLite file
  # lives (core/database/app_database.dart).
  path_provider: ^2.1.4
  # Bundles the native SQLite library for Android/iOS/macOS/Windows/Linux so the
  # Drift NativeDatabase has a sqlite3 engine at runtime.
  sqlite3_flutter_libs: ^0.5.24

dev_dependencies:
  # Drives Dart code generation (json_serializable). Generated *.g.dart files
  # are committed and never hand-edited (regenerate with:
  # `dart run build_runner build --delete-conflicting-outputs`).
  build_runner: ^2.4.13
  # Generates Drift database/DAO code (*.g.dart) from the table/accessor
  # annotations.
  drift_dev: ^2.20.0
  flutter_test:
    sdk: flutter
  # Generates fromJson for the flashcard API DTOs.
  json_serializable: ^6.9.0
  # Strict lint ruleset; activated via analysis_options.yaml.
  very_good_analysis: ^10.2.0

flutter:

  # Generate the gen-l10n AppLocalizations from lib/l10n/*.arb (see l10n.yaml).
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
