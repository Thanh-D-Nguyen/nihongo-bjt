# 16 — Operations / Feature Flags / Dead Letter

<context-hint>
Use for operations pages and backend enforcement of feature flags/kill switches.
</context-hint>

<task>
Act as DevOps + Backend + Admin UI Agent. Harden operations module.
</task>

<instructions>
1. Read `docs/spec/index.md`, `docs/spec/digests/devops_digest.md`, `docs/spec/compact/09_operations_ci_cd.md`, and `docs/spec/compact/10_testing_acceptance.md`.
2. Inspect feature flag service, admin operations pages, dead-letter queue.
3. Ensure feature flags are enforced backend-side.
4. Add admin UI for feature flags/dead-letter/content versions if missing.
5. Add tests for feature flag enforcement.
</instructions>
